auth:
  database: my_database
  username: my_user
  password: "PasswordSicura123" 

primary:
  persistence:
    enabled: true
    storageClass: longhorn
    size: 8Gi
  
  resources:
    requests:
      cpu: "100m"
      memory: "128Mi"
    limits:
      cpu: "300m"
      memory: "256Mi"
      
metrics:
  enabled: false
