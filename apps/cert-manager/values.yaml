replicaCount: 1

resources:
  requests:
    cpu: "50m"
    memory: "64Mi"
  limits:
    cpu: "200m"
    memory: "128Mi"

# Installiamo CRD
installCRDs: true

# Ottimizzazione repliche
webhook:
  replicaCount: 1
  resources:
    limits:
      memory: "64Mi"
cainjector:
  replicaCount: 1
  resources:
    limits:
      memory: "128Mi"
